const API_CONFIG = {
    // Đường dẫn gốc tới Server Spring Boot (mặc định là 8080)
    // Lưu ý: Không có dấu gạch chéo ở cuối
    BASE_URL: 'http://localhost:8080/api', 
    TIMEOUT: 10000,
};

export default API_CONFIG;